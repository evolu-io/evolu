import { useGetReferenceKey } from './useGetReferenceKey';

test('useReferenceKey works', () => {
  // TODO: Add all tests.
  // https://reactjs.org/docs/hooks-faq.html#how-to-test-components-that-use-hooks
  // https://jestjs.io/docs/en/tutorial-react
  expect(useGetReferenceKey).toBeInstanceOf(Function);
});
