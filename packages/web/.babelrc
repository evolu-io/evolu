{
  "presets": [
    "next/babel",
    [
      "@emotion/babel-preset-css-prop",
      {
        // Normalize sourceMap and autoLabel both for the production and the dev
        // so integration tests are always the same.
        "sourceMap": false,
        "autoLabel": true
      }
    ]
  ]
}
