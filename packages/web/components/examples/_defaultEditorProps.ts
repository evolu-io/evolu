import * as editor from 'evolu';

export const defaultEditorProps: Partial<editor.EditorProps> = {
  style: {
    backgroundColor: '#eee',
    marginBottom: 24,
    outline: 'none',
    padding: 8,
    width: 300,
  },
};
