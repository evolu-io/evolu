import { EditorProps } from 'evolu';

export const defaultEditorProps: Partial<EditorProps> = {
  style: {
    backgroundColor: '#eee',
    marginBottom: 24,
    outline: 'none',
    padding: 8,
    width: 300,
  },
};
