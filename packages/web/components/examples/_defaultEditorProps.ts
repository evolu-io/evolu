import * as editor from 'slad';

export const defaultEditorProps: Partial<editor.EditorClientProps> = {
  style: {
    backgroundColor: '#eee',
    marginBottom: 24,
    outline: 'none',
    padding: 8,
    width: 300,
  },
};
